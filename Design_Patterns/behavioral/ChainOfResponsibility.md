# Chain of Responsibility

Chain of Responsibility lets you pass a request along a chain of handlers.
Each handler decides either to process the request or pass it to the next.
(link list)

Chain of Responsibility is great when you want flexible delegation of tasks, without hard-wiring handler logic or 
dependencies.